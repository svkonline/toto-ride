# Toto Ride - Passenger App

## Setup

1. Install Flutter SDK.
2. Run `flutter pub get`.
3. Run `flutter run` to start the app on an Emulator or Device.

## Features

- Book rides
- Track drivers
- Rate trips
